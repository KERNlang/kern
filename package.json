{
  "name": "kern-monorepo",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "description": "Kern — the LLM-native language. Monorepo for @kern/* packages.",
  "author": "cukas",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/KERNlang/kern.git"
  },
  "homepage": "https://github.com/KERNlang/kern#readme",
  "license": "AGPL-3.0-or-later",
  "scripts": {
    "build": "tsc -b && pnpm --filter @kernlang/playground build",
    "test": "pnpm -r --filter '!@kernlang/review-python' test",
    "test:root": "node --experimental-vm-modules ./node_modules/jest/bin/jest.js --forceExit --config jest.config.js",
    "fitness": "pnpm test"
  },
  "devDependencies": {
"@types/jest": "^30.0.0",
    "@types/node": "^22.0.0",
    "jest": "^30.3.0",
    "ts-jest": "^29.3.0",
    "typescript": "^5.7.0"
  }
}
