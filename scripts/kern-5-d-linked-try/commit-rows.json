{
  "format": "kern.oracle.d-linked-try.commit-rows.v1",
  "abortCriterion": {
    "cut": false,
    "gated": 43,
    "preceding": 180
  },
  "criteriaCount": 61,
  "groups": {
    "Linker — admission and structure": [
      "type-gate.test.mjs",
      "probe-matrix.test.mjs"
    ],
    "Linker — payload typing": [
      "type-gate.test.mjs",
      "behavior.test.mjs"
    ],
    "Control flow": [
      "type-gate.test.mjs",
      "behavior.test.mjs",
      "finally.test.mjs"
    ],
    "RT-1 leg": [
      "walker-coverage.test.mjs",
      "compatibility.test.mjs"
    ],
    "JavaScript leg": [
      "walker-coverage.test.mjs",
      "compatibility.test.mjs"
    ],
    "Uncaught → public result": [
      "fault-carrier.test.mjs",
      "compatibility.test.mjs"
    ],
    "Metering and abort": [
      "metering.test.mjs",
      "fault-carrier.test.mjs"
    ],
    "Reserved labels and registry": [
      "reserved-labels.test.mjs"
    ],
    "Parity ledger": [
      "python-deferral.test.mjs"
    ],
    "Finally (D5, gated)": [
      "finally.test.mjs",
      "commit-rows.test.mjs"
    ]
  },
  "rows": {
    "D1": [
      "F5 owns the try-family refusals the linker cannot reach",
      "StatementWalkResult gains a threw variant, so an uncaught throw is a completion not an exception",
      "a bare print alone in a try body is projection-rejected, and the wrapped idiom is not",
      "a print and a capability committed before an uncaught throw both survive into the failure envelope",
      "a program with no throw and no try carries no __UserThrow token at all",
      "a resolved capability before an uncaught throw leaves the throw as the failing code",
      "a try around a helper call that completes normally runs the try body and never enters the catch",
      "an explicitly written code: null is admitted, because writing the default is not a refusal",
      "an omitted code reads back as an explicit null rather than taking the missing-member path",
      "an uncaught throw produces the frozen failure envelope byte-identically on both legs",
      "both fault code sets gain uncaught-throw and nothing else",
      "both label helpers fail closed on a non-text message rather than reading through it",
      "both legs produce the same clamped label for the same payload",
      "both position dispatchers wire the two new catalogues, so no row falls through",
      "both target kernels keep the digests D.0 pinned, because D adds no kernel byte",
      "callHelper given a walk that completes threw faults closed with KIR_TRY_FAMILY_IN_HELPER",
      "clampThrowLabel clamps the message to 256 code units and appends a 64-unit code in brackets",
      "clampThrowLabel is exported from the RT-1 evaluator, so both legs can share one label rule",
      "commit-rows.json exists, is canonically serialized, and keys exactly the five commit tags",
      "e.message and e.code link and evaluate, and e.missing keeps the existing missing-member fault",
      "every acceptance-criteria group in the spec is claimed by at least one oracle file",
      "every frozen row stays inside the signed 64-bit range the number model reserves",
      "every label with no spentBy entry appears in neither built kernel",
      "every label with no spentBy entry appears in no file under packages/core/src",
      "every oracle file in this suite stays under the 500-line ceiling",
      "every positive, twin and metering fixture projects with zero diagnostics",
      "every row name is claimed by exactly one commit tag, so the mapping is disjoint",
      "every source file D edits stays under its own line budget and the 500-line ceiling",
      "every spent label is genuinely emitted from statements.ts, so no spend is on paper only",
      "maxSteps exhausted inside a try with a catch produces runtime-limit-exceeded, catch skipped",
      "member expressions stay statically untyped on both channels, so reading a payload needs no typing change",
      "meter-for-3-try-leaf charges exactly 6 steps more than its hand-counted twin twin-for-3-leaf",
      "meter-try-leaf charges exactly 2 steps more than its hand-counted twin twin-leaf",
      "meter-try-throw-catch-leaf charges exactly 4 steps more than its hand-counted twin twin-let-and-leaf",
      "neg-assign-catch-binding is refused at link with KIR_ASSIGN_TARGET_NOT_LET",
      "neg-throw-bare is refused at link with unsupported property set",
      "neg-throw-boolean-literal is refused at link with KIR_THROW_PAYLOAD_SHAPE",
      "neg-throw-empty-record is refused at link with KIR_THROW_PAYLOAD_SHAPE",
      "neg-throw-extra-key is refused at link with KIR_THROW_PAYLOAD_SHAPE",
      "neg-throw-identifier is refused at link with KIR_THROW_PAYLOAD_SHAPE",
      "neg-throw-integer-literal is refused at link with KIR_THROW_PAYLOAD_SHAPE",
      "neg-throw-integer-message is refused at link with KIR_THROW_PAYLOAD_SHAPE",
      "neg-throw-let-bound-payload is refused at link with KIR_THROW_PAYLOAD_SHAPE",
      "neg-throw-list is refused at link with KIR_THROW_PAYLOAD_SHAPE",
      "neg-throw-member is refused at link with KIR_THROW_PAYLOAD_SHAPE",
      "neg-throw-nested-record-message is refused at link with KIR_THROW_PAYLOAD_SHAPE",
      "neg-throw-no-message is refused at link with KIR_THROW_PAYLOAD_SHAPE",
      "neg-throw-null-message is refused at link with KIR_THROW_PAYLOAD_SHAPE",
      "neg-throw-shadowed-binding is refused at link with duplicate binding e",
      "neg-throw-text-literal is refused at link with KIR_THROW_PAYLOAD_SHAPE",
      "neg-throw-with-children is refused at link with statement must be a leaf",
      "no JSON.stringify is reachable from the label path on either leg",
      "no class extends __Fault or _Fault, and __UserThrow is a bare nominal class",
      "normalize.ts stays the single internalRuntimeFailure producer, diverging only on events",
      "spentBy names exactly the labels D emits, each attributed to this slice",
      "statementSubExpressions gains exactly one arm, for try and not for throw",
      "the CI tier contract names the evidence leaf, so test:ci-contract cannot go red on it",
      "the D-7f abort comparison is recorded with the integers the mapping carries",
      "the D.0 aggregate row has been moved from last to before its declared successors",
      "the D.0 diagnostics rows that D moves have actually been moved, not left stale",
      "the D.0 lands-unspent row has been moved, because D spends into it",
      "the F5 projection policy digest does not move, because all four nodes already project",
      "the JavaScript __Fault census gains exactly one emitter site, for the uncaught conversion",
      "the JavaScript block dispatcher emits a native try and a nominal guard rather than a signal object",
      "the KernKirFault census equals D.0 plus exactly the three runtime sites D adds",
      "the Python _Fault census does not move at all, because the Python emitter is byte-frozen",
      "the Python compiler directory stays at exactly seven files, so the emitter is untouched",
      "the Python expression lowering table is unchanged, because D adds no expression kind",
      "the Python statement deferral switch keeps its never guard and names throw and try explicitly",
      "the Python statement lowering marks exactly the five ledger kinds as deferred",
      "the RC-v1 governed surface is untouched: three amendments and 125 receipt bindings",
      "the RT-1 evaluator finishes under its 470-line headroom, not merely under the ceiling",
      "the absent code default is inserted, so every linked payload carries both keys sorted",
      "the async driver converts an entry threw and fails closed on a helper threw",
      "the committed F5 probe matrix is the measured one, so a shrunken probe is noticed",
      "the compiled core inventory stays at the attested 357 paths, because D adds no source file",
      "the contract wall check-rule-coverage.mjs passes, so the public diagnostic surface holds no surprise",
      "the contract wall check-runtime-contract-v1.mjs passes, so the public diagnostic surface holds no surprise",
      "the contract wall check-runtime-envelope.mjs passes, so the public diagnostic surface holds no surprise",
      "the diagnostic code union stays at thirteen members and the envelope shape does not widen",
      "the emitted KIR token set under kir-runtime equals the recounted list exactly",
      "the emitted catch guards on instanceof and never on a fault code field check",
      "the emitted module for a throw, a try and helpers declares exactly one __UserThrow class",
      "the evidence leaf builds core and runs every oracle file in this suite exactly once",
      "the evidence leaf is wired into the aggregate exactly once, after the D.0 leaf",
      "the exhaustiveness scrape names both new kinds, so the surface pin cannot drift",
      "the five cascade digests are still at their base value, and every one of them must move",
      "the four new emitted labels are raised and are not registry members",
      "the frozen table names a fixture and a commit tag for every row, with no duplicate name",
      "the ledger carries exactly five rows, sorted by nodeKind",
      "the ledger document validates and its digest equals the checked-in hash",
      "the ledger-aware gate rows move from three to five, for both the rows and the kinds",
      "the linked expression union gains no member, because the payload rides the record kind",
      "the linked statement union carries throw and try and no other new kind",
      "the mapping is total: every row the suite actually runs is named exactly once",
      "the probe matrix is canonically serialized, so a hand edit is visible in the diff",
      "the reserved label set is still exactly the pinned eight, sorted",
      "the three neighbour goldens D scrapes nothing from do not move",
      "the throw and try rows carry the honest dependency order and this slice spec path",
      "this slice spec exists on disk at the path every ledger row cites",
      "throw at for-body links, emits JavaScript, and is refused on the Python leg",
      "throw at handler-top-level links, emits JavaScript, and is refused on the Python leg",
      "throw at if-else links, emits JavaScript, and is refused on the Python leg",
      "throw at if-then links, emits JavaScript, and is refused on the Python leg",
      "throw-uncaught links on RT-1 and the JavaScript leg",
      "throw-uncaught-coded links on RT-1 and the JavaScript leg",
      "try-payload-helper-call links on RT-1 and the JavaScript leg",
      "try-payload-helper-call returns the frozen integer 2 byte-identically on both legs",
      "try-payload-parameter links on RT-1 and the JavaScript leg",
      "try-payload-parameter returns the frozen integer 2 byte-identically on both legs",
      "try-throw-in-catch links on RT-1 and the JavaScript leg"
    ],
    "D2": [
      "WalkFrame gains a trap field and TryTrap carries the binding and both clause bodies",
      "a catch body that is never entered charges nothing at all",
      "a caught throw truncates the frame stack to the trap depth and does not resume the loop",
      "a deferral is Python-only, so the same try program still compiles to JavaScript",
      "a failing capability inside a try with a catch stays an uncatchable fault on both legs",
      "a member miss on the catch binding keeps the existing missing-member fault on both legs",
      "a throw with no trap frame returns threw and terminates instead of spinning unmetered",
      "a timeout inside a try produces execution-timeout with the catch body skipped, on both legs",
      "both purpose-built position catalogues exist and omit helper-body",
      "both semantic walkers traverse a hand-built try without throwing and reach its clause bodies",
      "cancellation inside a try produces execution-cancelled with the catch body skipped, on both legs",
      "catch and finally are try clauses, not union members, so the union gains exactly two kinds",
      "compileBlock dispatches try to a dedicated visitor rather than exempting it from assertLeaf",
      "containsReturn stays delegated to statementSubBlocks and re-learns no kind inline",
      "neg-catch-in-for is refused at link with KIR_CATCH_WITHOUT_TRY",
      "neg-catch-in-while is refused at link with KIR_CATCH_WITHOUT_TRY",
      "neg-catch-top-level is refused at link with KIR_CATCH_WITHOUT_TRY",
      "neg-try-body-after-clause is refused at link with KIR_TRY_BODY_AFTER_CLAUSE",
      "neg-try-duplicate-catch is refused at link with KIR_DUPLICATE_CATCH",
      "neg-try-empty-body is refused at link with branch block is empty",
      "neg-try-empty-catch is refused at link with branch block is empty",
      "neg-try-named is refused at link with unsupported property set",
      "neg-try-no-catch is refused at link with KIR_TRY_REQUIRES_CATCH",
      "no tracked kern file reaches the link stage, so admitting the try family moves no census row",
      "rt12 own statementSubBlocks pin gains try, which is the scrape the refactor relocated",
      "statementSubBlocks gains a try arm that reaches every clause body",
      "the RT-1 evaluator still carries exactly two checkAbort sites, and the try family adds none",
      "the STILL_OUTSIDE lists are untouched, because the try family was never in them",
      "the catch binding reads message and code as ordinary member expressions on both legs",
      "the catch body runs exactly on the rows the table marks, and on no other",
      "the helper-body position is a link refusal on all legs, which is why the catalogues omit it",
      "the ledger own recomputed walker visits the try clause blocks, so a nested throw is not missed",
      "the try family is refused in a helper body while the entry handler admits it",
      "try at for-body links, emits JavaScript, and is refused on the Python leg",
      "try at handler-top-level links, emits JavaScript, and is refused on the Python leg",
      "try at if-else links, emits JavaScript, and is refused on the Python leg",
      "try at if-then links, emits JavaScript, and is refused on the Python leg",
      "try-await-helper-completes links on RT-1 and the JavaScript leg",
      "try-await-helper-completes returns the frozen integer 1 byte-identically on both legs",
      "try-catch charges identically on RT-1 and the JavaScript leg at every budget",
      "try-catch links on RT-1 and the JavaScript leg",
      "try-catch returns the frozen integer 1 byte-identically on both legs",
      "try-catch-caught-throw charges identically on RT-1 and the JavaScript leg at every budget",
      "try-catch-caught-throw links on RT-1 and the JavaScript leg",
      "try-catch-caught-throw returns the frozen integer 2 byte-identically on both legs",
      "try-catch-coded-throw links on RT-1 and the JavaScript leg",
      "try-catch-coded-throw returns the frozen integer 2 byte-identically on both legs",
      "try-catch-null-code links on RT-1 and the JavaScript leg",
      "try-catch-null-code returns the frozen integer 2 byte-identically on both legs",
      "try-catch-reads-code links on RT-1 and the JavaScript leg",
      "try-catch-reads-message links on RT-1 and the JavaScript leg",
      "try-catch-return-in-body links on RT-1 and the JavaScript leg",
      "try-catch-return-in-body returns the frozen integer 5 byte-identically on both legs",
      "try-catch-return-in-catch links on RT-1 and the JavaScript leg",
      "try-catch-return-in-catch returns the frozen integer 6 byte-identically on both legs"
    ],
    "D3": [
      "rethrow is lexical only, and a payload record is not an admissible parameter type",
      "try-catch-no-binding links on RT-1 and the JavaScript leg",
      "try-catch-no-binding returns the frozen integer 1 byte-identically on both legs",
      "try-nested links on RT-1 and the JavaScript leg",
      "try-nested returns the frozen integer 1 byte-identically on both legs",
      "try-rethrow links on RT-1 and the JavaScript leg"
    ],
    "D4": [
      "a break inside a while inside a try inside a for is admitted and advances the outer loop",
      "for-try-break returns the frozen integer 1 byte-identically on both legs",
      "for-try-continue charges identically on RT-1 and the JavaScript leg at every budget",
      "for-try-continue returns the frozen integer 3 byte-identically on both legs",
      "for-try-guarded-continue returns the frozen integer 3 byte-identically on both legs",
      "try-for-break charges identically on RT-1 and the JavaScript leg at every budget",
      "try-for-break returns the frozen integer 0 byte-identically on both legs",
      "try-while-in-for-break returns the frozen integer 2 byte-identically on both legs"
    ],
    "D5": [
      "a bare break inside a try at handler top level is refused for its loop depth, not the cross-try label",
      "a failing capability inside a try with a finally skips the finally body on both legs",
      "a finally clause in a loop body is refused by F5, so only the if-wrapped shape reaches the linker",
      "a finally event is charged against maxBytes on both legs, not emitted past it",
      "a for inside a finally is refused by F5 while a while inside one reaches the linker",
      "a jump crossing a finally-bearing try is refused with the cross-try label",
      "a loop inside a finally-bearing try still permits its own break, because the depths are equal",
      "a return crossing a finally builds the envelope after the cleanup, with the cleanup event kept",
      "a return from the innermost body runs both finally bodies in order before the envelope",
      "a while inside a finally is admitted, which is the linker half of the F5 asymmetry",
      "for-inside-finally-bearing-try-break returns the frozen integer 3 byte-identically on both legs",
      "for-inside-finally-bearing-try-break runs its finally body exactly once and returns the frozen 3",
      "if the finally commit is cut, the cross-try label is spent nowhere and keeps no spentBy entry",
      "meter-try-leaf-finally-leaf charges exactly 3 steps more than its hand-counted twin twin-try-catch-leaf",
      "neg-catch-after-finally is refused at link with KIR_CATCH_AFTER_FINALLY",
      "neg-duplicate-finally is refused at link with KIR_DUPLICATE_FINALLY",
      "neg-finally-break is refused at link with KIR_ABRUPT_FINALLY_UNSUPPORTED",
      "neg-finally-continue is refused at link with KIR_ABRUPT_FINALLY_UNSUPPORTED",
      "neg-finally-nested-throw is refused at link with KIR_ABRUPT_FINALLY_UNSUPPORTED",
      "neg-finally-return is refused at link with KIR_ABRUPT_FINALLY_UNSUPPORTED",
      "neg-finally-stray is refused at link with KIR_FINALLY_WITHOUT_TRY",
      "neg-finally-throw is refused at link with KIR_ABRUPT_FINALLY_UNSUPPORTED",
      "rt12 own cross-try scan is retired rather than widened, because it was already complete",
      "the KIR_TRY_REQUIRES_CATCH key is deleted once the finally commit lands, never nulled",
      "the emitted finally body appears exactly once, not duplicated per exit edge",
      "the emitted lowering carries an outer guard, so a fault inside the catch body also skips the finally",
      "the emitted try region introduces no suspension point the finally-less twin does not have",
      "the finally body runs on a rethrown exit, and the throw still escapes as uncaught",
      "the finally body runs zero times on an exhausted step budget, even with a catch present",
      "the finally body runs zero times on cancellation and on timeout",
      "the importer defensive-loop shape behaves byte-identically on both legs with no finally",
      "the kernel-owned wrapper finally is unmoved, so the user finally is a separate mechanism",
      "try-finally returns the frozen integer 3 byte-identically on both legs",
      "try-finally runs its finally body exactly once and returns the frozen 3",
      "try-finally-caught-throw returns the frozen integer 3 byte-identically on both legs",
      "try-finally-caught-throw runs its finally body exactly once and returns the frozen 3",
      "try-finally-no-catch returns the frozen integer 3 byte-identically on both legs",
      "try-finally-no-catch runs its finally body exactly once and returns the frozen 3",
      "try-finally-return-in-body returns the frozen integer 5 byte-identically on both legs",
      "try-finally-return-in-body runs its finally body exactly once and returns the frozen 5",
      "try-finally-while returns the frozen integer 3 byte-identically on both legs",
      "try-finally-while runs its finally body exactly once and returns the frozen 3",
      "try{}finally{} with no catch links only once the finally commit lands"
    ]
  }
}
