# KERN 5 R2 M4.9 — Targeted Validator Structured Parameters

**Status:** SEALED
**Date:** 2026-07-20
**Confidence:** 0.99

## Executive Summary

[VERIFIED] Immutable M4.8 commit
`7f58cf8ec850f3b0144b13bf2751f0b4ce5c335a` records 16 of 104 corpus
functions base-complete, 86 functions blocked by the excluded legacy
`fn.params` property, all eight remaining candidate families at zero
completions, and a `null` winner (`scripts/kern-canonicalizer/coverage-summary.json`).

[VERIFIED] Exact read-only in-memory migration measurement on that commit found
only four remaining functions that become base-complete when their existing
legacy signatures are represented as ordered direct `param` children. Three
belong to one coherent source boundary in
`examples/selfhost-validator/validator.kern`: `charoknext`, `localname`, and
`failline`. They require six direct parameter rows and complete three
functions. The only other completion is one one-parameter canonicalizer helper,
`validnext`.

[VERIFIED] M4.9 therefore migrates only the three validator functions. It
advances base completion to 19 of 104 and reduces `fn.params` blockers to 83
without changing any candidate family or canonicalizer executable source.

## M4.8 Baseline / Root Cause

[VERIFIED] `validator.kern` is 465 lines and contains 21 functions, all using
non-empty legacy `params=` signatures (`wc -l` plus parser inventory on
2026-07-20). The target signatures are at
`examples/selfhost-validator/validator.kern:11`, `:111`, and `:117`:

| Function | Ordered parameters | Current rows | Migrated rows | New blockers |
|---|---|---:|---:|---|
| `charoknext` | `c:string` | 7/9/58 | 8/11/61 | none |
| `localname` | `alias:string`, `imported:string` | 5/7/25 | 7/11/31 | none |
| `failline` | `code:string`, `subject:string`, `rowId:number` | 3/5/58 | 6/11/67 | none |

[VERIFIED] The read-only measurement imported the authenticated live coverage
receipt, parsed every corpus member, replaced each legacy signature only in
memory, recomputed canonical profile rows, and checked completion against the
current base profile. Its result contained exactly the three validator rows
above plus `canonicalizer-expression-helpers.kern#1:validnext` at 6/9/53.

[VERIFIED] The root cause is representational, not behavioral: each target's
only excluded property is `fn.params`, and its migrated projection has no
structural or profile blocker. Existing mixed-mode guards permit direct-form
functions to coexist with internally consistent legacy-form siblings while
rejecting both forms inside one callable.

## What Already Works

[VERIFIED] The self-host validator executes its live module through
`use path="./validator"` and compares all 39 verdict lines to the TypeScript
reference; its generated `main.kern` contains fixture arrays rather than an
embedded validator-source snapshot
(`scripts/selfhost-validator/gen-fixtures-kern.mjs:88-125`,
`scripts/check-selfhost-validator.mjs:18-35`). Its bytes therefore need not
change for this signature-only edit.

[VERIFIED] The checker corpus does embed the live validator source through
`repoFile` (`scripts/capstone-checker-subset/fixtures.mjs:114-123`) and flattens
that source into its generated fixture
(`scripts/capstone-checker-subset/gen-fixtures-kern.mjs:13-42`). The checker
`main.kern` must therefore be regenerated by its repository generator.

[VERIFIED] The numeric checker fixture is generated from a separate frozen
case list (`scripts/capstone-checker-subset/gen-fixtures-kern.mjs:45-60`) and
must remain byte-identical.

## Contract

> Verified against immutable M4.8 source and authenticated coverage measurement on 2026-07-20.

| Behavior | Contract | Evidence | Tag |
|---|---|---|---|
| Exact source scope | Rewrite only `charoknext`, `localname`, and `failline` signatures | `validator.kern:11,111,117` | VERIFIED |
| Parameter order | `c`; `alias, imported`; `code, subject, rowId` | current legacy signatures and in-memory projection | VERIFIED |
| Parameter types | `string`; `string,string`; `string,string,number` | current legacy signatures | VERIFIED |
| Source isolation | Other 18 functions and every body/call/export remain byte-equivalent | exact source-diff oracle | VERIFIED |
| Mixed-module validity | Three direct-form targets coexist with 18 legacy siblings | existing representation guard plus exhaustive partition oracle | VERIFIED |
| Validator behavior | Preserve all 39 validator verdict lines | `package.json:35`, `scripts/check-selfhost-validator.mjs` | VERIFIED |
| Checker behavior | Preserve 48/48 parity and reject all 36 hostile attempts | `package.json:34`, `scripts/check-capstone-checker-subset.mjs` | VERIFIED |
| Coverage result | Exactly 19/104 complete and 83 legacy blockers | exact in-memory measurement | VERIFIED |
| Selection result | Eight families remain at zero and winner remains `null` | targets become base-complete without family facts | VERIFIED |
| Frozen ownership | Canonicalizer source, composition, profile, schema, registry, and promotion provenance remain unchanged | coverage handoff contract | VERIFIED |

## Implementation Options

| Scope | Functions | Params | New completions | Source result | Decision |
|---|---:|---:|---:|---:|---|
| Three exact validator functions | 3 | 6 | 3 | 471 lines | Select: coherent highest-yield boundary |
| `validnext` canonicalizer helper | 1 | 1 | 1 | 166 helper lines | Reject: reopens composition/executable evidence for one completion |
| One validator target | 1 | 1-3 | 1 | 466-468 lines | Reject: fragments one verified six-row boundary |
| Entire validator module | 21 | 102 | 3 | 567 lines | Reject: same gain and violates the 500-line source ceiling |

No external pre-implementation challenge is required: current evidence
collapses the decision to one bounded non-canonicalizer source boundary and
confidence is above the canonical 0.90 threshold.

## Implementation Plan

1. Add RED assertions for the exact 471-line source, 21 functions, three target
   IDs, six ordered direct parameters, 18 legacy siblings, exact migrated
   profile rows, 19 base completions, 83 `fn.params` blockers, and null winner.
2. Prove RED fails on immutable M4.8 before editing source.
3. Rewrite only the three target headers and add six direct `param` children.
4. Prove the old authenticated corpus digest rejects specifically on
   `validator.kern`; update only that digest, regenerate checker `main.kern`
   through its repository generator, and rewrite the authenticated coverage
   summary.
5. Run validator, checker, canonicalizer, complete Node 22 fitness, and
   automatically routed terminal review. Stop before `validnext` migration or
   any family promotion.

## Blast Radius

| File | Action | Reason |
|---|---|---|
| `examples/selfhost-validator/validator.kern` | Modify three signatures | remove the exact parameter-only blockers |
| `examples/capstone-checker-subset/main.kern` | Regenerate | checker corpus embeds validator source |
| `scripts/kern-canonicalizer/coverage-parameter-migrations.mjs` | Extend exact migration assertions | RED and permanent boundary evidence |
| `scripts/kern-canonicalizer/coverage.test.mjs` | Pin 19/104 and 83 | live authenticated totals |
| `scripts/kern-canonicalizer/coverage-policy.json` | Update validator digest only | authenticate changed corpus member |
| `scripts/kern-canonicalizer/coverage-summary.json` | Regenerate | authenticated receipt |
| `scripts/check-kern-canonicalizer-coverage.mjs` | Pin current totals | standalone coverage gate |
| `scripts/kern-canonicalizer/coverage-handoff.test.mjs` | Pin current totals | frozen provenance handoff |
| `docs/kern-5-release-train.md` | Record M4.9 | release evidence |
| this spec | Seal measured result | durable contract |

## Acceptance Criteria

- [x] RED fails on the unchanged 465-line source before implementation.
- [x] Migrated source is exactly 471 lines with 21 functions: three exact
      direct-form targets, six direct params, and 18 legacy-form siblings.
- [x] Target names, types, and order match the contract; all non-signature
      source remains unchanged.
- [x] Old policy digest fails specifically on `validator.kern` before only that
      corpus digest changes.
- [x] Authenticated coverage is exactly 19/104 with 83 `fn.params` blockers and
      the three exact target rows above.
- [x] All eight candidate families remain at zero and winner remains `null`;
      corpus/tool/function counts remain 9/4/104.
- [x] Canonicalizer source, composition, schema, profile, family registry,
      executable, and immutable promotion provenance remain unchanged.
- [x] Validator behavior remains 39/39; checker parity remains 48/48 and all
      36 hostile attempts remain rejected.
- [x] Validator `main.kern` and checker numeric fixture remain byte-identical;
      checker `main.kern` changes only through repository generation.
- [x] Focused gates and the complete Node 22 `pnpm fitness:kern-5` wall pass on
      the exact implementation tree.
- [x] Automatically routed terminal Agon review has no unresolved material
      finding.

## Measured Result

[VERIFIED] RED on immutable M4.8 failed with both `16 !== 19` and
`465 !== 471`. The source-only edit then failed the old authenticated policy
exactly with `corpus member examples/selfhost-validator/validator.kern digest
drift`. The migrated source SHA-256 is
`a46ab0d4376777f00180dfa8dcded00d2559770c57793773988ade29d542587a`.

[VERIFIED] Repository generation produced checker fixture SHA-256
`0f783b80fcff7116ea837a04b1d7ddfe298eb49f47b5f8e580b7968130de5e8a`.
Validator `main.kern` remained byte-identical at
`9ac7774a50ad9bcb7852340baf6844f130066f7eb004aa3b56e1974ce2a469b7`,
and the numeric checker fixture remained byte-identical at
`4bef89f9e64ab8a5e8aa0341bce3a28d1b77439e496fd19e4d7da1194182de4a`.

[VERIFIED] Authenticated measurement records 19/104 base completions, 83
legacy blockers, the three exact target profiles, eight zero-completion family
rows, and a null winner. The full canonicalizer gate passed all 67 tests plus
21 golden/idempotence/KIR fixtures, seven measured witnesses, three
profile-limit fixtures, and 140 hostile fixtures. Validator behavior remained
39/39 byte-identical. Checker adapter tests passed 3/3; runtime behavior
remained 48/48 byte-identical and all 36 hostile attempts were rejected.

[VERIFIED] The complete Node 22 `pnpm fitness:kern-5` wall passed, including
repository consistency, lint, production build, every workspace and
infrastructure suite, 432 cross-target fixtures, 109 class fixtures, 233
native assertions at 100% coverage, 39 validator verdicts, 40 whole-app
fixtures across three legs, browser budget, KIR/runtime guards, source-runner
convergence, and repeated canonicalizer evidence at 19/104 with 83 legacy
blockers and no selected tranche.

[VERIFIED] Full-roster terminal review
`review-1784577771218-s0mruf-kern-5-r2-m4-9-terminal-boundary` completed all
six usable engines with zero verified, needs-check, or speculative findings.
Its five nits do not change the contract: exact line counts, source order,
function ordinals, and literal profile rows are intentional fail-closed receipt
evidence. The one accurate wording nit is resolved by naming the 465-line
section as the immutable M4.8 baseline. No material finding remains unresolved.
This post-review sealing metadata was not input to its own review.

## Stop Conditions

- Completion gain or blocker reduction is not exactly three.
- Any non-target validator source bytes change, a fourth function must migrate,
  or any function mixes legacy and direct parameter forms.
- Validator `main.kern` or numeric checker fixture bytes change.
- A candidate family becomes selectable or a previously complete function
  regresses.
- Canonicalizer executable/composition or frozen promotion evidence changes.
- `validator.kern` is not exactly 471 lines or crosses the 500-line ceiling.
- Any validator, checker, hostile, focused, or full-wall gate fails.

## Out of Scope

- Migrating the other 18 validator functions.
- Migrating `validnext` or another canonicalizer function.
- Removing global legacy-parameter compatibility.
- Changing validator decisions, parser/checker/runtime behavior, canonicalizer,
  schema, profile limits, candidate families, or selection rules.
- Selecting, implementing, or promoting a canonicalizer family.

## Deploy Order

[VERIFIED] Source, generated checker fixture, corpus digest, authenticated
receipt, tests, spec, and release evidence ship atomically after fetch/rebase
and one feature push. No skewed deployment is supported.
