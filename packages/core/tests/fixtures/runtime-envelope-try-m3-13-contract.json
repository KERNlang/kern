{
  "format": "kern.runtime.try-m3-13-contract.internal.r1",
  "currentEvidence": "executable-machine-acceptance",
  "cases": [
    { "id": "preflight-body-before-capability", "category": "preflight", "description": "Unsupported protected-body structure rejects before capability dispatch." },
    { "id": "preflight-catch-before-capability", "category": "preflight", "description": "Unsupported unselected catch structure rejects before protected-body capability dispatch." },
    { "id": "preflight-finally-before-capability", "category": "preflight", "description": "Unsupported finally structure rejects before protected-body capability dispatch." },
    { "id": "catch-binding-replaces-throw-and-tombstones", "category": "catch-binding", "description": "Catch observes a literal canonical message, replaces throw, and tombstones its name." },
    { "id": "finally-preserves-normal", "category": "finally-preservation", "description": "Normal body runs cleanup and remains normal." },
    { "id": "finally-preserves-return", "category": "finally-preservation", "description": "Cleanup runs before preserving a return value." },
    { "id": "finally-preserves-uncaught-throw", "category": "finally-preservation", "description": "Cleanup runs before preserving an uncaught canonical throw." },
    { "id": "finally-preserves-caught-throw", "category": "finally-preservation", "description": "Catch replaces throw and cleanup preserves the catch completion." },
    { "id": "abrupt-finally-return-rejects", "category": "abrupt-finally", "description": "Cleanup-only finally rejects an unconsumed return before effects." },
    { "id": "abrupt-finally-throw-rejects", "category": "abrupt-finally", "description": "Cleanup-only finally rejects an unconsumed throw before effects." },
    { "id": "abrupt-finally-break-rejects", "category": "abrupt-finally", "description": "Cleanup-only finally rejects an unconsumed break before effects." },
    { "id": "abrupt-finally-continue-rejects", "category": "abrupt-finally", "description": "Cleanup-only finally rejects an unconsumed continue before effects." },
    { "id": "capabilities-in-body-catch-finally", "category": "capability-parity", "description": "Body, catch, and finally capabilities resume with raw sync and async parity." },
    { "id": "try-inside-counted-for", "category": "try-in-loop", "description": "Try remains machine-owned inside counted for and shares its budget." },
    { "id": "try-inside-array-each", "category": "try-in-loop", "description": "Try remains machine-owned inside array each and shares its budget." },
    { "id": "try-inside-while", "category": "try-in-loop", "description": "Try remains machine-owned inside while and propagates loop control through cleanup." },
    { "id": "loop-inside-try-body", "category": "loop-in-try", "description": "A counted loop executes in the protected body." },
    { "id": "loop-inside-try-catch", "category": "loop-in-try", "description": "A counted loop executes in the selected catch." },
    { "id": "loop-inside-try-finally", "category": "loop-in-try", "description": "Array each executes in finally without leaving the machine." },
    { "id": "break-through-finally", "category": "loop-control", "description": "Break propagates through normal cleanup and is consumed by the owning loop." },
    { "id": "continue-through-finally", "category": "loop-control", "description": "Continue propagates through normal cleanup and is consumed by the owning loop." },
    { "id": "nested-try-in-body", "category": "nested-try", "description": "Nested try executes in a protected body without fallback." },
    { "id": "nested-try-in-catch", "category": "nested-try", "description": "Nested try executes in a selected catch without fallback." },
    { "id": "nested-caught-try-in-finally", "category": "nested-try", "description": "A nested caught throw completes normally inside finally." },
    { "id": "return-with-catch-direct-rejects", "category": "return-with-catch", "description": "Direct protected return with catch rejects before effects." },
    { "id": "return-with-catch-if-rejects", "category": "return-with-catch", "description": "Protected return in if with catch rejects before effects." },
    { "id": "return-with-catch-else-rejects", "category": "return-with-catch", "description": "Protected return in else with catch rejects before effects." },
    { "id": "return-with-catch-branch-rejects", "category": "return-with-catch", "description": "Protected return in branch with catch rejects before effects." },
    { "id": "return-with-catch-for-rejects", "category": "return-with-catch", "description": "Protected return in counted for with catch rejects before effects." },
    { "id": "return-with-catch-each-rejects", "category": "return-with-catch", "description": "Protected return in array each with catch rejects before effects." },
    { "id": "return-with-catch-while-rejects", "category": "return-with-catch", "description": "Protected return in while with catch rejects before effects." },
    { "id": "return-with-catch-nested-try-rejects", "category": "return-with-catch", "description": "Protected return through nested try with catch rejects before effects." },
    { "id": "finally-loop-consumes-break", "category": "cleanup-analysis", "description": "A loop inside finally may consume break and leave cleanup normal." },
    { "id": "provider-failure-tombstones-sync-catch", "category": "provider-cleanup", "description": "Sync provider failure remains uncaught while catch binding is tombstoned." },
    { "id": "provider-failure-tombstones-async-catch", "category": "provider-cleanup", "description": "Async provider rejection remains uncaught while catch binding is tombstoned." }
  ]
}
