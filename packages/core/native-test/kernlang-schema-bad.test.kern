test name="KERNlang schema bad-case self contracts" target="./kernlang-schema-bad.kern" coverage=false
  it name="native tests can assert expected schema failures"
    expect has=schemaViolations count=1 matches="'expect' requires"
