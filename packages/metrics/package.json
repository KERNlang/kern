{
  "name": "@kernlang/metrics",
  "version": "3.5.3",
  "description": "Kern language metrics and project context export",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "keywords": [
    "kern",
    "llm",
    "metrics",
    "analytics",
    "transpiler",
    "ai"
  ],
  "license": "AGPL-3.0",
  "scripts": {
    "build": "tsc -b",
    "test": "pnpm run build && node --test --test-reporter=dot tests/*.test.ts"
  },
  "dependencies": {
    "@kernlang/core": "workspace:*"
  }
}
